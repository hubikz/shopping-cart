default:
    suites:
            default:
                path: %paths.base%/features
                contexts: [FeatureContext]
    extensions:
        Behat\Symfony2Extension: ~
        Behat\MinkExtension:
            sessions:
                my_session:
                    symfony2: ~